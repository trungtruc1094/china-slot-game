# Imports

Screenshots were supplied in the chat session rather than as local files:

1. Cash Out modal in Telegram Mini App shell: title `CASH OUT`, subtitle `Select an amount to withdraw`, presets `100 / 200 / 500 / 1000 / 2000`, amount entry, `Receive:` calculated field, `CASH OUT NOW` CTA, `1% withdrawal fee applies` note, close button, dimmed game background, bottom HUD visible.
2. Deposit modal in Telegram Mini App shell: title `DEPOSIT`, subtitle `Select an amount to deposit`, presets `100 / 200 / 500 / 1000 / 2000`, amount entry, `DEPOSIT NOW` CTA, `Secure & Encrypted` note, close button, dimmed game background, bottom HUD visible.

These screenshots are visual references, not authoritative behavior specs. DESIGN.md and EXPERIENCE.md are the spine contracts.
